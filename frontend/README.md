# Frontend

React frontend for the ourRAG MVP.

This directory will contain the thin client for workspace selection, chat, source rendering, and admin workflows. Product UI and business behavior are intentionally not implemented in this skeleton phase.

Initial local commands:

```sh
npm install
npm run dev
npm test
```

Vite loads environment files from the repository root. Required frontend variables use the `VITE_` prefix, for example `VITE_APP_ENV` and `VITE_API_BASE_URL`.
