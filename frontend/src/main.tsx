import React from "react";
import { createRoot } from "react-dom/client";

createRoot(document.getElementById("root") as HTMLElement).render(
  <React.StrictMode>
    <main>
      <h1>ourRAG</h1>
      <p>Local development frontend placeholder.</p>
    </main>
  </React.StrictMode>,
);
