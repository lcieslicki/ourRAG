# Infrastructure

Local infrastructure and deployment support for the ourRAG MVP.

The first skeleton includes Docker Compose services used by the backend during local development. Application container builds can be added once backend and frontend entry points exist.
